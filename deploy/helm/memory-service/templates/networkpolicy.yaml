{{- if .Values.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: planeon-memory-service-deny-all
spec:
  podSelector:
    matchLabels: {app.kubernetes.io/name: planeon-memory-service}
  policyTypes: [Ingress, Egress]
  ingress: []
  egress: []
{{- end }}
