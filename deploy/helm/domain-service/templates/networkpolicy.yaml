{{- if .Values.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: planeon-domain-service-deny-all
spec:
  podSelector:
    matchLabels: {app.kubernetes.io/name: planeon-domain-service}
  policyTypes: [Ingress, Egress]
  ingress: []
  egress: []
{{- end }}
