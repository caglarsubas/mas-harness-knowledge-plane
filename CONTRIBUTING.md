# Contributing

Changes require an authorized packet, a `codex/<packet-id>-<slug>` branch, signed
deny-all-outbound acceptance, and a green ephemeral self-hosted pull request.
Never put credentials, tenant data, source content, mutable images, or runtime
downloads in source or CI. Apache-2.0 contributions must be original or carry
explicit path-level reuse authority.
